to connect to sepolia network create local.properties file in the backend/blockchain_monitor/src/main/resources.
the file should define the infura.url property, for example:
infura.url=<your_sepolia_url>